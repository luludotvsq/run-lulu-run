# Run, Lulu, Run Plan

## Milestone 45 - Audit and docs reset

- Inspect the current codebase and document the current structure for:
  - rendering and camera
  - scene flow
  - HUD and overlays
  - asset loading
  - sprite sizing
  - tile sizing and world/screen sizing
  - audio
  - config layout
  - single-player versus multiplayer separation
  - duplication, dead code, and brittle paths
- Update:
  - `SPEC.md`
  - `PLAN.md`
  - `PROGRESS.md`
  - `AGENTS.md`
  - `ASSET_INTAKE.md`
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally
  - log the baseline findings and assumptions in `PROGRESS.md`

## Milestone 46 - Bounded maintainability pass

- Refactor the client structure conservatively to reduce brittleness:
  - separate app boot/layout from scene/session logic
  - reduce the monolithic `client/src/main.ts`
  - isolate HUD/menu/title flow concerns
- Introduce a real asset intake structure and manifest/fallback path for:
  - characters
  - environment
  - audio
- Centralize tunable client-side config for:
  - visual scale
  - layout
  - audio defaults
  - asset keys and fallback behavior
- Keep gameplay behavior unchanged unless support work is required for later milestones.
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally
  - update `PROGRESS.md` with structural changes and rationale

## Milestone 47 - Balance retune and intake integration

- Slightly reduce Springtrap base speed while keeping Springtrap faster than LULU.
- Put old and new values in config and document them in `PROGRESS.md`.
- Organize the supplied art assets into the new intake structure.
- Wire directional LULU sprites correctly.
- Wire Springtrap, NPC, floor, wall, rock, car, pallet, and gate assets where present.
- Keep placeholders or procedural fallbacks for missing categories.
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally
  - update `ASSET_INTAKE.md`
  - update `PROGRESS.md`

## Milestone 48 - Rendering readability and presentation pass

- Replace the dashboard-like launch presentation with a game-first experience.
- Create a real title screen with displayed title `Run, Lulu, Run`.
- Ensure the game visually owns the screen on launch.
- Adjust display scale so characters and environment art read clearly.
- Keep collisions and gameplay honest if display scale diverges from collider size.
- Preserve crisp nearest-neighbor rendering.
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally
  - run the Playwright loop and inspect screenshots
  - update `PROGRESS.md`

## Milestone 49 - Music integration and final regression pass

- Add title-screen music handling.
- Add gameplay music handling.
- Gracefully handle browser autoplay restrictions.
- Swap or stop music cleanly on scene transitions.
- If only one track is available, use the safest fallback and document it.
- Finalize:
  - `ASSET_INTAKE.md`
  - `PROGRESS.md`
  - `README.md` if startup flow or runtime setup materially changed
- Validation:
  - run `npm run check`
  - run the app locally
  - run the Playwright loop and inspect screenshots
- verify:
  - launch goes to the title screen
  - title text reads `Run, Lulu, Run`
  - the game no longer looks like a dashboard
  - Springtrap is still faster but less punishing
  - art remains crisp
  - music unlock and transitions work cleanly

## Milestone 52 - Alternating gameplay music, strict custom rotation, and deploy prep

- Copy the supplied second gameplay music track into the project asset intake path and document the copied location.
- Keep title music fixed, but alternate gameplay music between two round tracks:
  - the active round track loops if it ends before the round does
  - the next round switches to the other gameplay track only after a real round end
- Make runtime play strict and custom-only:
  - require exactly 2 valid custom maps in `maps/custom/`
  - block single-player start, room create, room join, and rematch when the requirement is not met
  - alternate the two valid custom maps in deterministic id order
  - keep the built-in QA map available only as an editor template
- Remove stale autogenerated custom-map files from the runtime path so they cannot join rotation accidentally.
- Extend the map-catalog response so the client can surface clear readiness and error messages.
- Move client/server URL resolution out of shared gameplay config and make production hosting work from one public origin.
- Make the server:
  - bind to `process.env.PORT` on `0.0.0.0`
  - serve the built client in production mode
  - keep local split dev flow intact
- Update docs for:
  - strict two-map authoring workflow
  - Render Free Web Service handoff
  - Springtrap AI versus human timing verification
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally in both split-dev and production-style server modes
  - run the Playwright loop and inspect screenshots
  - verify:
    - gameplay music alternates across completed rounds only
    - title music stays fixed
    - runtime play is blocked unless exactly 2 valid custom maps exist
    - only the 2 valid custom maps alternate in rotation
    - production serving works from one origin
    - AI Springtrap still uses the same attack recovery and ledge vault timing as human Springtrap

## Milestone 53 - World bounds, 10-generator objective, and artwork splash screen

- Keep actors fully inside the playable world:
  - stop LULU, NPCs, and Springtrap from visually sliding off-screen at map edges
  - keep the fix shared-engine-driven so both single-player and multiplayer inherit it
- Raise the live objective to 10 generators:
  - spawn 10 live generators per round
  - keep selecting them from authored generator spawn points randomly on each map load
  - only open the gate after all 10 live generators are repaired
  - keep the HUD, prompts, docs, and map-authoring guidance aligned with the new count
- Preserve per-round variety on the current custom map pair:
  - keep `forest` above the live count
  - add extra authored generator spawn points to `yard-custom` so it can randomize instead of always using the same 10 spots
- Replace the plain black splash with the newly supplied artwork:
  - copy the supplied splash art into the runtime intake folder
  - place the title in the top-left quadrant
  - place the action buttons in the bottom-right quadrant
  - keep both character illustrations unobstructed
  - keep the title/gameplay-first look and avoid dashboard framing
- Update docs for:
  - the 10-generator objective
  - the custom-map authoring expectation that more than 10 spawn points gives more variation
  - the new copied splash-art asset
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally
  - run a browser smoke check and inspect the splash layout
  - verify:
    - actors stay fully inside map bounds
    - 10 generators appear in live rounds
    - the gate stays closed until all 10 are completed
    - the title art layout keeps both characters visible

## Milestone 54 - Mobile browser controls

- Make the live game playable on mobile browsers without requiring a physical keyboard.
- Add a client-side input bridge so keyboard and touch controls drive the same session APIs.
- Add an on-screen mobile control overlay for gameplay:
  - left-side virtual movement pad
  - right-side action button for tap/hold interactions
  - visibility limited to active rounds so menus stay clean
- Keep desktop keyboard controls fully intact.
- Update prompts and player-facing copy so action instructions make sense on both keyboard and touch.
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally
  - run the Playwright loop and inspect screenshots
  - verify:
    - desktop keyboard controls still work
    - mobile touch controls can move, attack, vault, drop pallets, and hold interactions
    - touch controls only appear during live gameplay

## Milestone 55 - Static map seam fix and pallet knockback

- Eliminate distracting tile seams on live devices:
  - stop floor and obstacle tiles from showing moving grid lines as the camera scrolls
  - keep the pixel-art look crisp while removing per-tile seam artifacts
- Make pallet hits feel heavier:
  - when a pallet stuns Springtrap, push him back 3 tiles opposite his travel direction
  - keep the knockback collision-safe so it does not launch through walls or off the map
  - keep the existing stun duration unless tuning is needed later
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally
  - run browser smoke checks and inspect screenshots
  - verify:
    - the floor no longer shows moving seam lines while the camera scrolls
    - pallet stuns still trigger reliably
    - Springtrap is pushed back approximately 3 tiles when space allows

## Milestone 56 - Mobile zoom suppression and clean randomized round spawns

- Stop mobile browsers from zooming the live game when players double tap:
  - treat the issue as browser gesture handling, not a Render-hosting problem
  - disable accidental page zoom and gesture scaling during the game UI flow
  - preserve normal tap/touch gameplay input for menus and live controls
- Randomize clean round-start spawns for Lulu and Springtrap:
  - choose fresh start points each round instead of always using the authored fixed pair
  - keep both starts away from map edges so the mobile stick does not cover Lulu at spawn
  - keep Lulu and Springtrap meaningfully separated so rounds start clean
  - keep NPC spawns and map-editor compatibility intact
- Keep the spawn logic shared-engine-driven so both single-player and multiplayer inherit it.
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally
  - run the Playwright loop and inspect screenshots
  - verify:
    - double tap no longer zooms the page on the live gameplay shell
    - live rounds still accept touch input after the zoom guard
    - Lulu and Springtrap spawn from randomized interior points on both custom maps
    - round starts no longer place Lulu under the mobile joystick area

## Milestone 57 - Mobile round-music continuity

- Fix the mobile browser music regression where round music can stop after the first replay.
- Treat the issue as a mobile media-lifecycle problem:
  - avoid spawning fresh audio elements for later round transitions
  - keep the gameplay/title tracks warmed and reusable after the first user gesture
  - keep replay/rematch taps able to re-run the audio unlock path if a mobile browser suspends playback
- Preserve the existing audio behavior on desktop:
  - title cue stays fixed
  - gameplay tracks still alternate only after real round ends
  - the active round track still loops in place
- Validation:
  - run `npm run typecheck`
  - run `npm run build`
  - run the app locally
  - run the Playwright loop and inspect screenshots
  - verify:
    - mobile-style sessions keep music active into round 2 and round 3
    - gameplay music still alternates `A -> B -> A`
    - the audio debug state stays unlocked and unpaused through replay transitions
